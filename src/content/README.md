# Long-form copy

This folder holds editorial copy snippets that are too long to live alongside data
in `src/data/proposal.ts`. Each file is a `.mdx`-style fragment exported as a React
component, imported by the relevant section.
