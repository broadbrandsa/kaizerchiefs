// Deprecated — tier framing removed. Replaced by InvestmentCalculator (calculator)
// + ExecutionPlan (line-item plan). Kept as an empty module so existing
// imports/build cache references resolve cleanly.
export {};
